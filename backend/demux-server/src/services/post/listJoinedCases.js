import { Joined } from '../../models';

/**
 * Get list of all joined cases confirmed from the blockchain
 * @returns {Joined[]}
 */

 export const listJoinedCases = async (req, res) => {
    try {
        console.log('Return List Joined Cases');
    } catch (err) {
        console.error(err);
    }
 };